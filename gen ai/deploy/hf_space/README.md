# HF space deploy

1. new streamlit space on huggingface
2. upload app.py + requirements.txt from this folder
3. pick cpu (free) or gpu if you have credits
4. first load downloads the model (~1gb for qwen 0.5b)

takes a while on cold start. fine for demo.
